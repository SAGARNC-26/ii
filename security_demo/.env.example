# Security Demo Environment
# Copy to .env and adjust values for your network and OS

# Server laptop IP (the machine running the dashboard and RTSP/Proxy)
SERVER_IP=192.168.1.10

# Plain RTSP (vulnerable) port on server
RTSP_PORT=8554

# RTSP Proxy listening port (dashboard should read from this)
RTSP_PROXY_PORT=8556

# RTSPS (TLS) port (TLS terminator forwards to RTSP_PORT)
RTSPS_PORT=8555

# SRT secure streaming
SRT_PORT=9000
SRT_PASS=secretkey

# Linux camera device (server)
CAMERA_DEVICE=/dev/video0

# Windows camera device name for dshow (server)
DSHOW_DEVICE=Integrated Camera

# Default upstream source (used by RTSP proxy)
# Typically the local RTSP webcam service running on RTSP_PORT
SOURCE_URL=rtsp://127.0.0.1:${RTSP_PORT}/live.sdp

# Network interface used by tshark on attacker laptop (e.g., eth0, en0, Wi-Fi)
IFACE=Wi-Fi

# TLS certificate details (for RTSPS)
TLS_CN=localhost
